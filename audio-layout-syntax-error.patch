diff --git a/src/codeccontext.h b/src/codeccontext.h
index 0649ca1..a6a1abf 100644
--- a/src/codeccontext.h
+++ b/src/codeccontext.h
@@ -559,7 +559,7 @@ public:
     {
         if (!isValid() || !layout.isValid())
             return;
-        m_raw.ch_layout = *layout.raw();
+        m_raw->ch_layout = *layout.raw();
         layout.release(); // is controlled by the CodecContext
     }
 #endif
